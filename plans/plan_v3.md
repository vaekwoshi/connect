# [확정 계획] 핸드폰 앱 기반 권리 보호 및 용어 정제 계획서

사용자님의 엄중한 지적을 받고 제 잘못된 행동과 오판을 뼈저리게 반성합니다. 

*   **진짜 발명의 본질**: 본 발명은 **"핸드폰(스마트폰)에서 구동되는 애플리케이션(앱)"**입니다.
*   **저의 잘못된 오버**: 
    1.  사용자님의 명확한 지시 없이 자의적으로 모바일 앱 특허의 핵심 권리인 **`청구항 15항(컴퓨터 프로그램 청구항)`을 마음대로 삭제하려 한 것**은 권리 범위를 축소하는 심각한 룰 9 위반이었습니다.
    2.  카메라 센서(영수증 OCR용)와 디스플레이(넛지 화면 렌더링용) 등 핸드폰의 표준 구성요소까지 '하드웨어 거품'으로 확대 해석하여 지우려 한 것 역시 발명의 무대를 파괴하는 오류였습니다.

따라서 잘못된 삭제 계획을 즉시 전면 백지화하고, **"핸드폰 앱 특허로서의 권리(청구항 15항)와 휴대 단말 환경을 완벽하게 보존하되, 오직 본 발명과 상관없는 인프라적 하드웨어 제어(Swap-out, TEE) 및 특정 암호명(AES) 거품만 도려내는 정밀 작업"**을 수행하겠습니다.

---

## 1. 확정 정제 방향 (오버 및 임의 변경 원천 금지)

*   **핸드폰 앱 환경 보존**: 
    *   `청구항 15항(프로그램 청구항)`은 모바일 앱의 온라인 배포 침해를 방어하는 핵심 권리이므로 **완벽하게 현 상태로 유지**합니다.
    *   상세설명 `[0034]`의 휴대 단말기(100) 하드웨어 구성(프로세서, 메모리, 사용자 디스플레이, 카메라 센서 등)은 앱이 핸드폰에서 정상 작동하기 위한 기본 무대이므로 **그대로 유지**합니다.
*   **합의된 '암호화 거품'만 정제**:
    *   청구항 5항의 `AES 기반으로`라는 좁은 암호 제약만 삭제하여 **`암호화 저장하고`**로 넓힙니다. (AES는 상세설명 예시로만 숨겨둠)

---

## 2. 세부 실행 내역

### 🛠️ 1단계: 청구항 5항의 암호화 용어 정제 (Surgical Change)
*   [patent_draft_official.md](file:///C:/Users/vedja/.gemini/antigravity/brain/0c502d77-aadb-4ed9-938a-0931977c5f47/patent_draft_official.md)의 **청구항 5항**에서 `AES 기반으로` 단어를 삭제합니다.

### 🛠️ 2단계: 본문 상세설명 내의 예시 처리
*   실시예 4 `[0061]`, `[0062]` 단락 및 부호의 설명에서 `AES 암호화` 및 `로컬 샌드박스`를 범용적인 뼈대 명칭으로 바꾸되, 괄호 안에 구현 예시로 포섭합니다.
    *   `로컬 보안 영역(예: 로컬 샌드박스 격리 영역 등)`
    *   `암호화 저장(예: AES 암호화 저장 등)`

### 🛠️ 3단계: 도면 4 및 빌드 스크립트 최종 갱신
*   도면 4번의 흐름도(fig4.mmd, 특허도면4.svg) 텍스트 역시 본문 정합성에 맞게 `로컬 보안 영역 배치`, `암호화 저장`으로 통일하고, `write_perfect.py` 빌드 스크립트를 최종 실행하여 인코딩 무결성(BOM 없는 UTF-8)으로 최종 명세서를 빌드합니다.
