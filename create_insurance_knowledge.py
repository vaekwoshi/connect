import os
import json

base_dir = r"c:\Users\vedja\.gemini\antigravity\scratch\세끌\지식_변환"
json_dir = os.path.join(base_dir, "JSON")
md_dir = os.path.join(base_dir, "마크다운")

os.makedirs(json_dir, exist_ok=True)
os.makedirs(md_dir, exist_ok=True)

# 1. 직장인
employee_json = {
    "직군": "직장인",
    "대상보험": "4대보험 (직장가입자)",
    "특징": "산재보험 제외 3대 보험료를 회사와 절반씩 부담",
    "계산공식": [
        {"항목": "국민연금", "본인부담금": "세전 보수월액 × 4.75%"},
        {"항목": "건강보험", "본인부담금": "세전 보수월액 × 3.595%"},
        {"항목": "장기요양", "본인부담금": "산출된 건강보험료 × 13.14%"},
        {"항목": "고용보험", "본인부담금": "세전 보수월액 × 0.90%"},
        {"항목": "산재보험", "본인부담금": "0원 (사업주 100% 부담)"}
    ]
}
employee_md = """# 직장인 4대보험 가이드
- **가입 자격:** 직장가입자
- **특징:** 산재보험을 제외한 3대 보험료를 회사와 근로자가 정확히 절반(50:50)씩 부담합니다.
- **국민연금:** 세전 보수월액 × 4.75%
- **건강보험:** 세전 보수월액 × 3.595%
- **장기요양:** 건강보험료 × 13.14%
- **고용/산재:** 고용 0.90%, 산재 0원
"""

# 2. N잡러
njob_json = {
    "직군": "N잡러",
    "대상보험": "건강보험 (소득월액보험료 추가 징수)",
    "특징": "부업 소득 2,000만원 초과분에 대해 건강보험료만 추가 부과. 회사 지원 없음 (본인 100% 부담).",
    "계산공식": [
        {"항목": "건강보험(추가)", "본인부담금": "{(연간 부업 소득 - 2,000만 원) ÷ 12개월} × 7.19%"},
        {"항목": "장기요양(추가)", "본인부담금": "추가 산출된 건강보험료 × 13.14%"}
    ]
}
njob_md = """# N잡러 추가 4대보험(소득월액보험료) 가이드
- **가입 자격:** 직장가입자 (부업 3.3% 프리랜서 소득 발생 시)
- **특징:** 본업 월급은 직장인과 똑같이 공제되며, 부업 소득은 2,000만 원을 초과할 때만 건강보험료가 추가 청구됩니다. 국민/고용/산재는 부과되지 않습니다.
- **건강보험(추가):** {(연간 부업 소득금액 - 2,000만 원) ÷ 12} × 7.19%
- **장기요양(추가):** 추가 건강보험료 × 13.14%
"""

# 3. 프리랜서
freelancer_json = {
    "직군": "전업 프리랜서",
    "대상보험": "국민연금, 건강보험 (지역가입자)",
    "특징": "소속 회사가 없으므로 본인이 100% 전액 부담",
    "계산공식": [
        {"항목": "국민연금", "본인부담금": "기준소득월액 × 9.50%"},
        {"항목": "건강보험", "본인부담금": "부과점수(소득+재산+자동차) × 점수당 단가(약 211.5원 등)"},
        {"항목": "장기요양", "본인부담금": "지역가입자 건강보험료 × 13.14%"},
        {"항목": "고용/산재", "본인부담금": "원칙적 0원 (노무제공자 예외)"}
    ]
}
freelancer_md = """# 전업 프리랜서 지역가입자 4대보험 가이드
- **가입 자격:** 지역가입자
- **특징:** 회사가 없으므로 본인이 100% 전액 부담합니다. 급여 기준이 아니라 종합소득세 신고 금액과 재산이 기준이 됩니다.
- **국민연금:** 신고된 기준소득월액 × 9.50% (본인 100% 부담)
- **건강보험:** 부과점수(소득+재산+자동차) × 부과점수당 금액
- **장기요양:** 산출된 지역건보료 × 13.14%
"""

# 쓰기
with open(os.path.join(json_dir, '직장인_4대보험_가이드.json'), 'w', encoding='utf-8') as f: json.dump(employee_json, f, ensure_ascii=False, indent=2)
with open(os.path.join(md_dir, '직장인_4대보험_가이드.md'), 'w', encoding='utf-8') as f: f.write(employee_md)

with open(os.path.join(json_dir, 'N잡러_4대보험_가이드.json'), 'w', encoding='utf-8') as f: json.dump(njob_json, f, ensure_ascii=False, indent=2)
with open(os.path.join(md_dir, 'N잡러_4대보험_가이드.md'), 'w', encoding='utf-8') as f: f.write(njob_md)

with open(os.path.join(json_dir, '프리랜서_4대보험_가이드.json'), 'w', encoding='utf-8') as f: json.dump(freelancer_json, f, ensure_ascii=False, indent=2)
with open(os.path.join(md_dir, '프리랜서_4대보험_가이드.md'), 'w', encoding='utf-8') as f: f.write(freelancer_md)

print("KNOWLEDGE BASE CREATED")
